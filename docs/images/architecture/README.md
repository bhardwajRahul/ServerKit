Hand-authored architecture diagrams for docs/ARCHITECTURE.md.

Each image here has a matching ASCII fallback inside a <details> fold in that
document. When you replace or add an image, keep the ASCII block in sync so the
doc still reads correctly in a plain-text viewer.

Naming: kebab-case, matching the section slug (system-overview.png,
request-flow.png, extension-platform.png, ...).

Not to be confused with docs/screenshots/, which is auto-captured by the
Playwright screenshots skill and should not be hand-edited.
